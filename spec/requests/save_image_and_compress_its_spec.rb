require 'rails_helper'

RSpec.describe "SaveImageAndCompressIt", type: :request do
  describe "GET /save_image_and_compress_its" do
    it "works! (now write some real specs)" do
      # get save_image_and_compress_its_path
      # expect(response).to have_http_status(200)
    end
  end
end
